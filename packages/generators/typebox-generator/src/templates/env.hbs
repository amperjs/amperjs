import type { TStringOptions, TNumberOptions, TArrayOptions } from "typebox";

declare global {
  type {{refineTypeName}}<
    T,
    O extends T extends string | RegExp
      ? TStringOptions
      : T extends number
        ? TNumberOptions
        : T extends unknown[]
          ? TArrayOptions
          : unknown,
  > = T;
}
